<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
  
<configuration>
	<!-- 全局setting配置 -->
	
	<!-- 配置别名 -->
	<typeAliases>
		<!-- 批量扫描 -->
		<package name="cn.ssm.po"/>
	</typeAliases>
	
	<!-- 配置mapper
		由于使用spring和mybatis的整合包进行mapper扫描，所以不需要配置
	 	必须遵循：mapper.xml文件和mapper.java文件在同一目录下，且文件名相同
	 -->
</configuration>